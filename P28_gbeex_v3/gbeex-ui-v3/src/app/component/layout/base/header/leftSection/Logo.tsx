import styles from "@/app/component/layout/base/header/leftSection/Logo.module.css";

const Logo = () => (
  <img src="/logo/logo.svg" alt="App Logo" className={styles.logo} />
);

export default Logo;
